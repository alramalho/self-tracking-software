[OUTPUT]
    name    http
    match   *
    tls     On
    host    ${BTRSTCK_INGESTING_HOST}
    port    443
    uri     /fluentbit
    header  Authorization Bearer ${BTRSTCK_SOURCE_TOKEN}
    header  Content-Type application/msgpack
    format  msgpack
    retry_limit 5
    net.dns.mode TCP