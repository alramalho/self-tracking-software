# Migration parity and validation

This inventory distinguishes implemented code from runtime verification. The backend has not been modified.

| Area | Implemented | Still to complete or verify |
| --- | --- | --- |
| Activity logging | Native tiles, activity CRUD and measure conversion, quantity/date, captions/private notes, photo picker/camera, location, friend selection, shared candidates, difficulty, follow-up metrics, entry edits/deletion | Real multipart/camera/location and shared logging against backend; visual comparison of every logging step; voice input and platform integrations |
| Timeline | Pagination, linked-entry grouping, two-column compact text cards with full-width expansion, photos/zoom, reaction picker, comments with full fetch, entry editor, share, collapsible rolling-week upcoming sessions, seen divider, shared-link highlight, achievement messages/editing/deletion | Achievement creation/celebrations; remaining coach/greeting home cards and coach interactions; device photo gestures |
| Plans | Native segmented heatmap/legend/day detail, current-week progress and week history, coach overview/two-week calendar, specific schedule toggle/details, long-press order, basic wizard/editor, milestones and progress, notes/pause/resume/archive/delete, visibility, background image upload/removal | Full plan wizard field parity and visual comparison, collaboration/invitations, coach proposals, real background upload/storage validation, recurrence/session generation; device drag and calendar validation |
| Profile | Original avatar/stat layout and progress ring, account totals/level/badges and points/streak rankings, friends list and connection actions, direct-chat navigation, virtualized history/plans, unplanned grid, theme/settings | Coach profile and coach creation flows, comprehensive account/settings integrations; actual connection/message/auth tests |
| Metrics | Check-ins, partial-save retry, daily note synchronization, 5-level heatmap/context markers, weekday insights, two-week trend, activity correlations, metric request | Free-user onboarding/preview and upgrade flow; native chart/scroll visual review; event data with backend |
| Authentication | Native Clerk AuthView with completion gating; web SignIn; token bridge; per-user cache | Native Clerk application/provider setup, sign-up/recovery/MFA/provider authentication and sign-out on device |
| Native navigation | Gesture-owned refresh state across all five screens, light/dark splash configuration, iOS Liquid Glass system tabs and Android Material tabs, scroll minimization, native refresh, router theme/insets | Simulator/device confirmation of appearance, keyboard/refresh/scroll behavior; deep-link and push navigation |
| Publishing | Expo config/prebuild, EAS profiles/project, restricted build archive, successful cloud simulator and signed physical iPhone builds, production environment, verified device provisioning and release bundle, build/submit scripts | Physical installation/authentication confirmation, remaining native API E2E, store submission |

Remaining legacy routes beyond the five priority screens include onboarding, insights onboarding, wrapped, join-plan, get-coached/create-coach-profile/manage-ai-coach, and remaining advanced coaching workflows. The native messages inbox and coach conversation now use the production participant and v2 streaming contracts. Search/notifications and other account integrations still need broader parity validation.

The Vite live-tracking hook and overlay currently have no call sites in the checked Vite application; their presence alone does not establish a shipped UI flow to port.

## Evidence and limitations

- Local fixture browser suite: all 25 cases passed across the full run and targeted rechecks after the September 12 design fixes, including achievement editing/deletion, shared-link highlighting, integer quantity validation, dark forms, rankings, two-column timeline expansion, weekly-summary navigation and multipart plan background upload/removal.
- Unit suite: 15 cases passed covering calendar/DST and date keys, grid intensity/pauses/history, shared projection, timeline grouping, skipped metrics, account totals, ordering and event baselines.
- September 12 display corrections: sign-in uses the existing 80-point “t” icon with one native heading, a centered form with 24-point side padding, system appearance when signed out, and a Clerk native theme with 16-point controls. Vite's timeline uses `rounded-2xl` (16 points), its badge component uses full pills, and its Add tiles use `rounded-lg` (8 points). Expo preserves 16-point timeline cards while increasing general cards/Add tiles to 20 points and controls to 16 points.
- Timeline cards now follow Vite's avatar/emoji/username/activity/date hierarchy, participant account rings, overlapping photo caption, overlaid reaction chips, compact comment composer, and joint participant clusters with individual activity/quantity rows. One/two participant avatars overlap horizontally; larger groups form a cluster. Photos deduplicate across participants, deleted rows and expired photos are excluded, and all owned rows remain editable. Captions support links and expansion. Rich URL preview cards are still outstanding.
- Photo viewing uses a full-screen navigation route with an iOS system toolbar close button, a safe-area fallback control on other platforms, outside-image dismissal, and iOS pinch zoom. The full five-screen iOS flow passed after the initial display corrections (`2026-09-12_084840`). The dedicated joint-photo flow remains **failing on the first close tap** in clean iOS simulator runs as of September 13. Later isolated taps and a run with instrumentation passed, and direct runtime inspection confirmed navigation itself can close the viewer; this does not establish a reliable fix. Physical-device verification is required for the new internal candidate. Do not count this flow as passing or the photo-viewer issue as closed.
- TypeScript passes. Production JavaScript exports passed for iOS, Android and web after the timeline and plan additions.
- Native compilation on the available Xcode 26.3 failed inside Expo's JSI dependency. Expo SDK 57 requires Xcode 26.4+. No dependency-source workaround was applied.
- Android debug compilation passed with JDK 17, API 36 and NDK 27.1. The full Maestro 2.9.0 flow passed on an ARM64 API 36 emulator: activity logging, timeline expansion, plan and profile day details with multiple activities, profile history, metric check-ins, pull-to-refresh with an API refetch assertion, and all five screens in light/dark mode. The runner also asserts persisted activity quantity/description and both metric ratings/note. Native testing exposed compact timeline cards stretching vertically and Axios sending an incorrect multipart content type; both are fixed and verified in the passing run. Screenshots are under `test-results-native/2026-09-11_164236/parity/takeScreenshot/`.
- Inter is bundled with four explicit weights for native text and inputs. Browser regression coverage checks the semibold face and compact-card dimensions. Low metric ratings retain their actual bar height, with unclipped labels above small bars. The restricted EAS archive independently installs with the frozen lockfile, passes TypeScript and exports iOS/Android/web bundles.
- Dark mode has explicit screen and form coverage. Inspected the original Vite and Expo screenshots, restored the profile glow, underlined profile tabs, achievement badge layout, weekday grid alignment and selected metric contrast. Browser screenshots do not validate iOS Liquid Glass appearance.
- The user authorized EAS source upload. Simulator build `111183e3-5e28-4d8c-959a-6b38132141dc` completed on September 12, 2026 with the supported cloud toolchain. The full iOS Maestro flow passed on iPhone 17 Pro / iOS 26.3: activity quantity/caption persistence, timeline expansion, plan and profile segmented day details, profile history, metric ratings/note persistence, real pull-to-refresh refetch, and all five screens in dark/light mode. Screenshots are in `test-results-native-ios/2026-09-12_082205/parity-ios/takeScreenshot/`. Verified native Liquid Glass expansion and scroll minimization. The simulator build cannot be installed on an iPhone.
- iOS automation launches the development client with its developer menu disabled, targets text inputs explicitly, dismisses the keyboard before saving metrics, and expands the minimized native tab bar before switching screens. The simulator runtime is missing `AppleColorEmoji.ttc` (confirmed by CoreText logs), so emoji appearance and related fallback text sizing cannot be visually approved from these screenshots. No app font workaround remains; physical-device appearance still needs confirmation.
- Physical iPhone build [`e8056981-36db-4b6f-9a1a-8decb912f90a`](https://expo.dev/accounts/alramalho/projects/tracking-so/builds/e8056981-36db-4b6f-9a1a-8decb912f90a) (build number 2) completed and the installation link was sent to the user on September 12, 2026. The downloaded IPA passed signature verification and assertions for the registered iPhone, production API/live Clerk configuration, disabled fixture authentication and bundled release JavaScript. It uses `device-production` and does not need Metro. During setup EAS briefly disabled HealthKit/App Groups on the shared identifier; both were restored and preserved in Expo configuration before provisioning. This does not implement those integrations in Expo.
- Production API health returns OK and Clerk's Native API is enabled, with email-code/password authentication configured. The first physical iPhone Google attempt exposed an empty production redirect allowlist. On September 12, 2026, `so.tracking.app://callback` was added through Clerk's Backend API after verifying the credential's JWKS matches the app's live Clerk instance. The installed IPA already registers this scheme, so no rebuild is required. The current `AuthView` Google button uses browser OAuth; the missing callback entry was the observed blocker, rather than a need for a separate Google native SDK. The user’s subsequent physical-phone screenshot confirms they reached an authenticated photo-viewer session; sign-up, recovery, MFA and sign-out still require validation. The production Clerk association endpoint is empty, so Apple/passkey native application setup remains outstanding.
- A separate simulator smoke check loaded the live production Clerk configuration with fixture authentication disabled and displayed the native sign-in screen successfully (`test-results-native-ios/production-signin.png`). After the redirect allowlist fix, Maestro tapped Continue with Google, accepted the iOS browser consent prompt, and verified Google's “Email or phone” sign-in page for tracking.so (`test-results-native-ios/production-google-provider.png`). No credentials were entered and no real production sign-in was completed by these checks. Build 2 predates the September 12 sign-in, card and photo-viewer styling changes.
- Real-backend browser E2E passed using a Clerk password session and the unchanged Express API, isolated PostgreSQL/pgvector database and Redis. Verified logging, editing, plan day details, profile history, metric check-ins and persistence directly in PostgreSQL. No native-device authentication/notifications/camera validation is claimed.
- Live validation exposed the backend's integer quantity contract; inputs now reject fractions instead of silently truncating. The isolated seed includes an accepted connection because the existing timeline endpoint returns an empty feed for users without connections.
- The backend attempted background coach-note generation on a plan-state transition and reported a missing AI Gateway credential. The core API operations passed; AI coach generation is not verified by this run.

The migration is not complete until the outstanding parity and native validation work is closed. Preserve `frontend-vite` and the existing backend throughout this parallel phase.

- September 13 candidate validation: TypeScript and all 15 unit cases passed; the three targeted joint-card/browser dismissal cases passed again in dark/light mode; production exports succeeded for all three platforms. Native production sign-in was loaded with the new Clerk theme and app icon, then visually inspected in dark and light mode (`test-results-native-ios/production-signin-design-{dark,light}.png`). No credentials were entered. The dedicated iOS first-close failure above remains open.

- September 13 internal iPhone candidate build 3 submitted: `6b382809-a2a3-4a9f-81da-071b493a3de7`, profile `device-production`, existing production backend and live Clerk configuration. It contains the new sign-in, card/radius, and native toolbar viewer changes. Submission is not installation readiness; artifact/provisioning checks and physical first-close verification remain pending.

- Build 3 completed September 13 and its downloaded IPA passed signature, registered-iPhone provisioning, release JavaScript, production API/live Clerk, fixture-disabled, preserved entitlements and Clerk radius checks. Install candidate: https://expo.dev/accounts/alramalho/projects/tracking-so/builds/6b382809-a2a3-4a9f-81da-071b493a3de7. This confirms installation readiness, not resolution of the first-close simulator failure or full migration parity.

- September 13 reaction correction: replaced the reaction sheet with an anchored translucent pill above the trigger, and restored photo-entry triggers to the lower-right photo overlay. Compared against the deployed `AchievementPostCard.CBYYdt7D.js` bundle on app.tracking.so, rendering its unchanged reaction selector in an isolated browser reference without signing in or writing production data. Production dimensions were 244×40 with seven 24-point emoji; native keeps the 244-point width/order and uses 48-point height for 44-point tall hit areas. Selection, dismissal, retries and narrow-window positioning passed browser checks; the existing reaction/comment contract also passed. The dedicated iOS flow passed in both themes including outside dismissal and four verified add/remove requests on the joint entry (`2026-09-13_165007/reactions-ios`). The simulator's missing emoji font remains an environment limitation; glyph appearance was compared in the browser reference, native positioning and interactions in the simulator.

- Reaction update submitted as internal production-connected iPhone build 4: `51420fe8-282f-4265-b69a-dd0c6890a18b`. All four relevant browser checks passed across the full run and Escape-key recheck; native dark/light reaction selection and dismissal passed. Package readiness will be verified after EAS completes.


## September 13 coach messaging candidate

- Fixed the Home Messages render crash: `/chats` serializes participant names, usernames and IDs directly, while the previous native screen dereferenced `participant.user`. The fixture now includes a real-shaped direct conversation so this regression is exercised.
- Rebuilt the inbox with the pinned AI coach, direct/group conversation previews, unread counts, people search and direct-chat creation. Added a native conversation list and fixed composer, keyboard avoidance, cross-chat coach history, visibility-based read receipts, current v2 SSE sending/status, direct sending, editing, failure/draft recovery, up to four image attachments and photo viewing.
- Added coach message feedback, failed-assessment retry, metric/activity/plan/context proposal accept/reject controls, plan-change previews, plan-creation change requests, markdown/entity links, sources, attention prompts, new conversations, assessment, confirmed history clearing, personality/weekly recap/reachout settings and context summary.
- Compared bubble styling with the deployed `index.BGbiBmE6.js` components by rendering the unmodified production `MessageBubble`/`MessageMarkdown` locally in a browser. No authenticated production conversation or production data was altered. Reference captures: `/private/tmp/tracking-coach-production-{dark,light}.png`; measured 24-point corners and 12×16 padding.
- All 15 existing unit tests passed. The eight combined browser checks passed before the final entity-link/accessibility adjustments, covering both themes, chat send/read contracts, editing, settings/new conversations, attachments, feedback, proposal acceptance, retry and reaction anchoring/dismissal/persistence. Final regression results are recorded below once complete.
- The dedicated iOS flow passed on iPhone 17 Pro / iOS 26.3 with the native development client: Home → inbox → coach send/response with keyboard → inbox → direct send → light theme → coach settings. It asserts actual fixture POST requests for coach v2 and direct messages. Evidence: `test-results-native-ios/2026-09-13_171633/messages-ios`. Header interactions wait for native transition/keyboard animations to settle before the next tap.
- This validation uses local fixtures for message writes; actual production model generation and device photo selection remain physical-device follow-up checks. The existing backend remains unchanged. Advanced coach proposal editing/context visualization still differs in detail from the web UI; this entry does not claim the entire migration is complete or close the previously documented photo-viewer first-close issue.

- Final candidate validation: 31 distinct browser cases passed across the regression run and targeted rechecks (27 initially, the three affected chat cases after correcting duplicate web button semantics, plus native entity-link navigation). All five coach messaging cases passed together in the final targeted run. TypeScript and 15 unit tests passed. Production exports completed for iOS, Android and web at `/private/tmp/tracking-expo-coach-export`. iOS native gesture buttons retain their tested implementation; the web wrapper uses a single semantic button.

- Combined internal production-connected iPhone build 5 submitted: `269822b8-c6b2-491c-8bbf-f9d1c04cb89e`. It includes the reaction island and coach messaging update. Install/package verification is pending EAS completion.

- Build 5 completed and its IPA passed production API/live Clerk, fixture-disabled, registered-iPhone provisioning, signature, preserved entitlements, release JavaScript and reaction/coach implementation marker checks. Ready for iPhone testing: https://expo.dev/accounts/alramalho/projects/tracking-so/builds/269822b8-c6b2-491c-8bbf-f9d1c04cb89e. Verification report: `/private/tmp/tracking-expo-coach-build-verified.json`. The previous reaction-only build 4 is superseded; its notification monitor remains paused.

## Comments interaction update — September 13, 2026

The timeline now uses a dedicated React Native comments sheet instead of the generic form drawer. It opens at 58%, expands to 94%, supports drag/backdrop/close dismissal, and keeps a virtualized list separate from the keyboard-following composer. The rounded surface follows the saved dark/light palette. Comment rows include avatars, relative dates, @mention reply shortcuts and compact, confirmed deletion. Drafts survive closing/reopening and failed requests; sending is disabled while pending. Replies remain ordinary @mention comments using the existing backend capabilities.

Activity, joint-entry and achievement comments retain their existing API paths. Ownership now checks the top-level `userId`: production comment responses omit `user.id`, which previously prevented deletion controls from appearing after fetching the full list. Fixtures now reproduce that response shape. No backend or Vite changes are required.

The sheet uses `@gorhom/bottom-sheet` with the existing native gesture/Reanimated dependencies. Its content is explicitly ungrouped for accessibility; native controls use gesture-handler touch targets. Deliberate dismissal uses `forceClose`, and a persistent native modal host avoids a close/reopen accessibility race. A modal-level safe-area provider keeps the composer above the physical home indicator rather than inheriting the tab-bar inset. Expo web uses a regular gesture-handler input because the library's native input blur handler calls an API absent from React Native Web.

Browser checks cover both themes, reply prefill, draft restoration, posting/deletion, failure/retry without duplication, achievement endpoints, and dragging open a 25-comment thread while the composer stays pinned. The existing reaction/comment contract and both reaction-island theme cases also pass. Fifteen existing unit tests pass. The complete iOS Maestro flow passes in `test-results-native-ios/2026-09-13_182450/comments-ios/`: dark/light keyboard spacing, one-tap close while typing, draft restoration and actual send persistence, expansion, swipe dismissal and backdrop dismissal. The final five browser comment cases also validate the persistent modal lifecycle; the prior three reaction/contract checks are retained. TypeScript passes. All test writes use isolated fixtures with the production response shapes; no comments were posted to real users during validation.

Production exports passed for iOS, Android and web (`/private/tmp/tracking-expo-comments-export`). Internal production-connected [iPhone build 6](https://expo.dev/accounts/alramalho/projects/tracking-so/builds/19eb9eca-b510-43ef-a824-d99b488e6fc3) completed at 17:36 UTC. The downloaded IPA passes signature verification, includes the registered iPhone, uses `https://api.tracking.so` and live Clerk, disables fixture authentication, bundles the new comments/reaction/coach code, and preserves HealthKit/App Group entitlements. Verification report: `/private/tmp/tracking-expo-comments-build-verified.json`.

## Coach overview plan links — September 13, 2026

`CoachOverview` now uses the native coach Markdown/entity renderer already used by chat, replacing plain text rendering that exposed `{{plan:id|label}}` syntax. Plan labels open the referenced plan; activity labels open the existing native activity preview. This also restores message formatting such as bold text. The backend and calendar layout are unchanged.

Three browser cases pass: both overview themes (including amber/Oli styling, multiple assessment messages, two distinct plan destinations and an activity preview), plus the existing chat entity-link regression. The iOS Maestro flow `--ios --plan-links` passes in both themes, including actual navigation to each plan and the activity preview (`test-results-native-ios/2026-09-13_184131/plan-links-ios/`). TypeScript and all-platform production export pass. Validation uses fixture messages matching the production entity syntax; it does not write coach messages to production.

Production-connected [iPhone build 7](https://expo.dev/accounts/alramalho/projects/tracking-so/builds/30a0e717-390d-4559-99c9-f470fc728ea8) completed at 17:51 UTC. The downloaded IPA passes signature, registered-device, production API/live Clerk, fixture-disabled and bundled-JavaScript checks, including preservation of the previous comments/reaction/coach changes and native entitlements. Verification report: `/private/tmp/tracking-expo-plan-links-build-verified.json`.

## Coach inline UI fidelity — September 13, 2026

Compared `message-ai.tsx`, `MessageEntityMarkdown`, `PlanLink`, `ActivityLink`, `MetricSuggestion`, and the proposal components in the Vite app. Native now resolves legacy `planReplacements` inside the sentence, using the same precedence as DSL references and metric suggestions. It no longer repeats plan references as full-width buttons below messages. Missing entities render as plain labels. The shared renderer also serves the plans overview.

Plan/activity references use compact, emoji-prefixed chips with the PWA's faded theme backgrounds. Tapping opens a dismissible native preview: plan activities, upcoming sessions, completion state, session guides and a separate full-plan action; activity previews include linked plans. Metric suggestions retain their inline rating and accept/reject preview, with errors leaving the suggestion reviewable. iOS does not expose embedded text attachments as independent accessibility controls, so mixed paragraphs use native text runs and separate controls, with a sentence label for VoiceOver and independently accessible chips.

Plan changes, plan creation, activity logs and activity edits now appear as compact review controls instead of always-expanded panels. Plan changes retain quick accept/reject actions and operation summaries; detailed review preserves the existing mutation endpoints. No-change plan proposals are skipped without changing their original proposal indices. This restores the conversation-level presentation; the advanced proposal calendar and full plan-edit form still have the broader migration limitations noted above.

The conversation header now matches the PWA's smaller arrow/eye/vertical menu icons and spacing, with the coach identity opening settings. Full-plan navigation uses a compact arrow/header and returns to the originating conversation instead of displaying a full-width Back button. No backend or Vite implementation changes were made.

Validation: 11 browser cases passed, plus the proposal/feedback/send-retry case repeated after the final native row-layout correction. Three reference-precedence tests and TypeScript pass. The iOS `--inline-coach` flow passes in dark/light, covering legacy and DSL chips, previews, session guides, metric review and return navigation (`2026-09-13_191252/inline-coach-ios`). The dedicated `--coach-actions` flow also passes in both themes, including review, dismissal and a verified activity-log acceptance request (`2026-09-13_191623/coach-actions-ios`). Native screenshots were inspected and exposed a collapsed action label despite successful tapping; the final layout correction and both-theme screenshots confirm the label is visible. The simulator's existing missing emoji font limitation remains; no font workaround was added to the release.

The all-platform production export passed at `/private/tmp/tracking-expo-inline-coach-export`. The subsequent internal iPhone build upload was rejected by EAS because the account's free monthly iOS build quota is exhausted (reset reported as October 1). No new build ID or installable artifact was issued. Existing build 7 does not include these inline changes. Local fallback is still unavailable with the installed Xcode 26.3; SDK 57 native compilation requires Xcode 26.4+. Submission log: `/private/tmp/tracking-inline-coach-build-submit.log`. Release packaging is ready to retry once EAS build capacity or a compatible local Xcode is available; a paid subscription was not purchased.

EAS incremented the remote build number from 7 to 8 before rejecting submission; the next auto-incrementing retry will normally be build 9. Verify the actual build number returned by EAS before checking the artifact.

## Profile header fidelity — September 13, 2026

Compared the Vite profile route, `BadgeCard`, `FireBadge`, `ProgressRing` and account-level styles. Earned profile badges now use byte-identical copies of the PWA's fire, seed and rocket Lottie assets, rendered natively at 65 points inside the same 50×62 crop. Badge frames use the original 12-point corner radius and two-point border ring; counts use the locally bundled Caveat font at 20 points. Unearned badges remain muted outlines. Native animations pause off-screen and respect reduced-motion settings.

Friends, Points and account level now occupy three equal-width columns with eight-point gaps, correcting the crown's excessive drift toward the right edge. Header labels, numbers, name and fallback initial match the PWA's sizes. The level icon uses the PWA's separate icon color instead of incorrectly sharing its ring/label color. Settings now uses the original 26-point vertical ellipsis and eight-point outer right inset. Badge, settings, account-progress and ranking actions are retained; backend and Vite code are unchanged.

The iOS `--profile-design` flow passed in both themes, including actual badge/progress/settings taps. Native screenshots inspected at `test-results-native-ios/2026-09-13_192527/profile-design-ios/` confirm the original animation artwork and balanced header spacing. Browser checks cover 320- and 390-point layouts in both themes plus unearned badges; geometry assertions verify equal columns, right insets and badge dimensions. Animation checks inspect painted canvas pixels rather than merely accepting an empty canvas. Profile model tests pass, including server-supplied account totals.

Final validation: five browser cases passed with painted-animation checks, three profile model tests passed, TypeScript passed, and all-platform production export passed (`/private/tmp/tracking-expo-profile-export`). The existing EAS iOS quota block remains; no paid plan was enabled and no new iPhone build was submitted for this follow-up. These profile changes are included in the prepared export alongside the pending coach-inline fixes.


### Local iPhone release and installation links — September 13, 2026

Build 11 (1.0.0) was compiled locally on Xcode 26.6 with iOS 26.5 platform components, verified and uploaded to private S3 storage. It contains the pending coach-inline/action/back-control and profile badge/header corrections described above, alongside earlier reaction/comment/coach fixes. No Expo cloud build quota was used and no subscription, OTA publication or store submission occurred.

The default `build:iphone` now performs local EAS compilation, IPA verification and private expiring installation-link publication. Five workflow tests, TypeScript, actual IPA signature/environment/device checks, feature-marker inspection, hosted download checks and installer browser inspection in dark/light mode passed. The user must still confirm physical iPhone installation. See BUILDING.md for the exact artifacts and renewal commands; signed URLs are stored only in ignored `.release/` output. Shared preference is installed globally and project instructions are saved for both apps. This does not complete the broader migration parity gaps listed above.

### Reaction people and glass badges — September 14, 2026

Tapping an existing reaction badge now opens a compact popover anchored to that badge, initially filtered to its emoji. The All filter groups each person once with their reactions; rows show avatars/names and open profiles. Close, outside tap and accessibility escape dismiss it. The bounded list scrolls for large groups. Viewing reactions never sends a reaction mutation. The smiley retains the separate anchored emoji picker for adding/removing reactions, now highlighting the user's selected emoji.

Photo reaction badges use native iOS clear Liquid Glass with a neutral translucent tint. Selected reactions use the same glass with an accent tint and border. Web uses backdrop blur; unsupported native platforms retain a translucent fallback. Non-photo badges keep the theme surface. Own, other-user and joint activity cards share the viewer, preserving existing backend endpoints and production response shapes, including missing nested user IDs.

Validation: ten browser cases passed across both themes, including own/other/joint activities, a narrow non-photo list, dismissal, profile navigation, zero mutations while viewing, and existing reaction add/remove/error-retry behavior. The identity grouping unit test and TypeScript passed. The iOS `--reaction-people` flow passed in dark/light with actual filtering, close/reopen, outside dismissal and profile navigation; its request log confirms zero reaction mutations. Native glass/popover screenshots were inspected in `test-results-native-ios/2026-09-14_072713/reaction-people-ios/`. The simulator's existing missing emoji font remains a simulator limitation; no font workaround was added. Tests use isolated fixtures and do not react to production users' activities.


Local production-connected build 12 (1.0.0) was compiled, signed, verified and hosted September 14. Actual bundled JS includes the reaction viewer controls; production configuration, registered iPhone, provisioning/signature and entitlements passed. The IPA, manifest and installer signed GET links all passed verification, and the hosted installer was checked in both themes with a matching build-12 native install link. See BUILDING.md for exact artifacts and September 21 link expiry. No cloud build quota was used. Physical installation of build 12 remains for the user to confirm.

## Streak exploration, year wrapped and Home reselect — September 14, 2026

The profile's fire, habit and lifestyle badges now open an iOS system Badge Details sheet with two detents with relevant plans, achievement celebrations and the PWA's original earning rules (including the missed-week buffer). Plan rows open their existing native plan details.

The own-profile wrapped entry opens the same 2025 retrospective as Vite: hero totals, country map, animated journey and memories, plan achievements, mood insights/calendar, activity totals and points/streak leaderboards. It uses native views/SVG/gestures, bundled original map geometry and the original heavy italic title font. Sharing captures the current story for the native share sheet (PNG download fallback on web). Photos have safe-area close controls and outside dismissal. Empty sections and failed requests have explicit states and retry behavior. Existing backend contracts and production environment are retained.

Home uses the router's scroll-to-top hook, and its FlatList is the first native child so the native tab bar can discover it on reselect. Double-tapping Home is covered alongside the new flows. These changes are included in verified local build 13; see BUILDING.md for its current hosted artifact.

The badge sheet uses a protected router route with native form-sheet presentation, grabber and rounded corners; the web version uses the same content in a bottom-aligned overlay. The system owns sheet dragging and dismissal, and the content exposes its controls individually to accessibility. Wrapped uses a full-screen native presentation, safe-area controls and 44-point story navigation targets. Native validation uses the matching iOS 26.5 runtime.

Validation: TypeScript, three wrapped-data model tests and four mobile browser E2E cases pass. The native iOS 26.5 flow passes from fresh launches in DARK (`2026-09-14_090613/wrapped-ios`) and LIGHT (`2026-09-14_090844/wrapped-ios`): sheet opening/scrolling/dismissal, all stories, country selection, plan filtering, photo dismissal, Home double-tap returning to the header, and the native PNG share sheet. Screenshots were inspected in both themes. Fixture data includes 36 year entries, mood ratings, multiple plans and a friend; empty/retry cases are covered in the browser. No production user data was mutated for these checks.

Maestro snapshots require settling before the next tap on this simulator/toolchain. The native flow now waits for animations after each capture and explicitly settles story-control taps. Direct coordinate checks confirmed the same controls worked; retain those waits rather than adding repeated taps or changing production button behavior to satisfy the driver.


Local production-connected build 13 (1.0.0) completed and was hosted September 14, 2026. The actual IPA passed production configuration, release signature, provisioning/device and entitlement checks, and contains the new badge/Wrapped/Home feature markers. The full hosted IPA hash matches the local verified artifact; installer and manifest GET checks pass, with build 13 and the native Safari install scheme verified in both themes. Download links expire September 21 at approximately 08:17 UTC. Exact artifact/report paths are in BUILDING.md; signed URLs stay in ignored `.release/`. No Expo cloud quota, OTA or store submission was used. The user still needs to confirm installation on their physical iPhone.

## Progressive reveals and Plans / Metrics polish — September 14, 2026

Compared Vite's `PlanRendererv2`, `PlanNotesBlock`, `SteppedBarProgress`, profile/Home animated sections, `MetricInsightsCard`, `CorrelationEntry`, and the actual theme CSS. Native sections now reveal once when entering the viewport (500 ms fade and 20-point rise), with short ordered delays. Seen IDs survive list virtualization and refresh. Scrolling checks only pending reveal sections, and native opacity/translation use the native animation driver. Reduced motion shows content immediately and stops completion pulses.

Selected plan details now sit directly on the page background instead of inside one encompassing card. Notes, milestones and current-week/calendar sections have their own 16-point rounded filled islands; the activity grid is outlined and transparent. The plan cover uses the original 192-point treatment and page-background fade; the title uses 24 points. Notes with content start expanded, with the original icon tile, compact header, divider, formatted headings/lists/emphasis/code/links, inline editing, preview, Save and Cancel. Failed saves retain the draft. Weekly and habit/lifestyle stepped progress strips return, including completion pulsing that pauses off-screen. The current-week action is a compact unfilled control.

Metrics insights now match the PWA's title, 24-point padding, 16-point corners, activity/reliability/percentage row, 12-point animated bars, reliability thresholds (5/15/30), subdued insufficient samples and neutral colors below 10% correlation magnitude. Reliability opens its sample-count explanation. Correlations use the same complete numeric history as the PWA instead of applying the current 1–5 heatmap filter to historical scales. One-day windows and the existing November 2025 timestamp cutoff are retained. No production values were hardcoded or production data mutated.

The original dark border is `hsl(0 0% 10.29%)` (#1a1a1a); native previously used #29292c, producing the heavy outlines in the user's screenshot. The border token now matches Vite, with a separate original input-border token for text fields. Light mode retains its corresponding borders. Reveal behavior is integrated into Home, Plans, Profile and Metrics; backend and Vite source are unchanged.

Validation: TypeScript and three correlation-history/reliability model tests pass. Four browser E2E cases pass, covering both themes, notes formatting/edit/cancel/save/failure recovery, island colors/radii, reliability details, measured intermediate fade frames, reveal-once behavior, completion pulse frames and live reduced-motion changes. Final iOS 26.5 runs pass in DARK (`2026-09-14_100537/polish-ios`) and LIGHT (`2026-09-14_100713/polish-ios`), including notes preview/cancel, Metrics reliability, profile/feed scrolling and Home double-tap. Screenshots were inspected in both themes. The native driver expands the minimized tab by tapping its current icon before switching; screenshot-settling waits remain. Test fixtures are isolated from production.

Local production-connected build 14 (1.0.0) was compiled, signed, verified and hosted September 14. Actual IPA checks passed for production configuration, registered device, release signature, provisioning and entitlements; the new feature markers are present in bundled JS. The full hosted IPA hash matches the verified local artifact, and the installer/manifest were checked in both themes with build 14 and a native Safari install link. Download links expire September 21 at approximately 09:14 UTC. See BUILDING.md for exact artifacts and renewal commands. No Expo cloud quota, OTA or store submission was used. Physical installation of build 14 remains for the user to confirm.

## Activity logging drawer fidelity — September 14, 2026

The native logger now follows Vite's actual `routes/add.tsx` → `ActivityLoggerPopover` flow: a content-sized rounded bottom drawer, centered 24-point title and 36-point activity emoji, timezone caption, inline month calendar, compact editable time, centered quantity with plus/minus controls, the original 10/30/45/60/90 presets and full-width action. Future days are disabled; native time selection uses the installed iOS wheel picker. The quantity remains directly editable for accessibility and arbitrary values. The close icon stays in the drawer's safe area, with backdrop and handle-swipe dismissal.

The logging drawer accounts for the iOS keyboard frame explicitly so quantity/caption editing stays below the status bar and scrolls within the available height. Number entry has a native Done accessory; multiline captions retain their native keyboard and scroll/tap dismissal. Friend selection lives with the optional photo/caption details and retains its selected user across Back/Next. Photo upload, location, private notes, shared-activity linking, difficulty and metric follow-ups keep their existing backend contracts. Dismissal is guarded while save/link/difficulty requests are pending. Backend and PWA files were not changed.

Validation and release status for this change are recorded in BUILDING.md. Browser checks use `e2e/logger.spec.ts` and the existing logging test in `e2e/parity.spec.ts`; the native runner's `--logger` flow exercises the iOS controls and asserts the resulting API request. Test data is isolated from production.


Build 15 (1.0.0) is compiled, signed, verified and hosted with production services. Its complete hosted IPA hash matches the local artifact, and the manifest and both installer themes were checked. The fresh install link expires September 21 at approximately 12:32 UTC; exact artifact paths are in BUILDING.md. TypeScript, three browser cases, both full native theme flows and the final status-bar smoke check pass. No Expo cloud quota, OTA or store submission was used.
